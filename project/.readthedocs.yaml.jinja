version: "2"

build:
  os: "{{ read_the_docs_os_target }}"
  tools:
    python: "{{ python_version }}"

python:
  install:
    - requirements: docs/requirements.txt

sphinx:
  configuration: docs/source/conf.py
